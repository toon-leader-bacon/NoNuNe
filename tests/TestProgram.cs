using Xunit;
using System.Collections.Generic;


namespace NoNuNe
{
    public class PrimeService_IsPrimeShould
    {
        [Fact]
        public void testIntToBinary() {
            // List<double> result = Program.intToBinary(0);
            // Assert.Equal(result, new List<double>{0, 0, 0, 0});
            
            // Assert.Equal(Program.intToBinary(1), new List<double>{0, 0, 0, 1});
            // Assert.Equal(Program.intToBinary(2), new List<double>{0, 0, 1, 0});
            // Assert.Equal(Program.intToBinary(3), new List<double>{0, 0, 1, 1});
            
            // Assert.Equal(Program.intToBinary(4), new List<double>{0, 1, 0, 0});
            // Assert.Equal(Program.intToBinary(5), new List<double>{0, 1, 0, 1});
            // Assert.Equal(Program.intToBinary(6), new List<double>{0, 1, 1, 0});
            // Assert.Equal(Program.intToBinary(7), new List<double>{0, 1, 1, 1});
            
            // Assert.Equal(Program.intToBinary(8), new List<double>{1, 0, 0, 0});
            // Assert.Equal(Program.intToBinary(9), new List<double>{1, 0, 0, 1});
        }
    }
}